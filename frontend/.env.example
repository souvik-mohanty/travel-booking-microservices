# api-gateway has no routes configured yet (see backend/gateway/api-gateway),
# so the frontend talks to each backend service directly on its own port.
# Switch these to the gateway's URL once routes exist there.
VITE_IDENTITY_SERVICE_URL=http://localhost:8081
VITE_TOUR_SERVICE_URL=http://localhost:8082
VITE_BOOKING_SERVICE_URL=http://localhost:8083
VITE_SEARCH_SERVICE_URL=http://localhost:8099
